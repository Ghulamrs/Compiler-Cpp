x86_64-linux  cxx1 emits both of Itanium's constructor forms and clang emits only the ones a use needs - see docs/CONFORMANCE.md, "An inline member function is a strong symbol"
arm64-darwin  same: Shape is only ever built as a base here, so clang writes its C2 and no C1, where cxx1 writes both
