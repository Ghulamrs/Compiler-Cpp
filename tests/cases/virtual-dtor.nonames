x86_64-linux  clang guards the allocation in `new Derived`: if the constructor throws, the memory operator new returned has to be freed, so it emits a cleanup - _Unwind_Resume and the personality reference. cxx1 emits no cleanup for a failed new, which is its own step. Darwin's clang emits none here either, which is why only this target differs
