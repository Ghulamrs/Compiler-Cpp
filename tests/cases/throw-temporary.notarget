x86_64-windows  a class-typed throw is refused for this target: the Microsoft ThrowInfo chain has to be pointed at a class - a type descriptor and a catchable type per class, and a _CTA array listing the thrown class and its bases - and cxx1 emits the four objects for a fundamental type only. Same lag throw-class.notarget records. Everything this case measures is the Itanium path.
