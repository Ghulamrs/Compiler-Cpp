x86_64-linux  the destructors and Poly's constructor are written inside their classes, so clang emits one comdat copy as C2/D2 and no C1 or D1 at all where cxx1 emits both of Itanium's forms - see docs/CONFORMANCE.md, "An inline member function is a strong symbol". arm64-darwin agrees on all 11 names and x86_64-windows on all 8, the deleting destructor ??_GPoly@@UEAAPEAXI@Z included, which is the one this case exercises through a null pointer
