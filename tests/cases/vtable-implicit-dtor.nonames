x86_64-linux  the constructors and destructors here are implicit or written inside their class, so clang emits one comdat copy as C2/D2 and no C1 or D1 where cxx1 emits both of Itanium's forms - see docs/CONFORMANCE.md, "An inline member function is a strong symbol". The names this case exists to check do agree: D's and E's destructors are emitted at all now, which is the whole point, and x86_64-windows agrees on all 17 including ??1E@@UEAA@XZ and ??_GE@@UEAAPEAXI@Z
arm64-darwin  same, and narrower: only B's C1, which is never called as a complete object here because every B is built as a base
