# Where this round stopped — 2026-09-06

**HEAD is `8681827` on `vector-exercise`, 252 commits, pushed.** Everything
described below as *done* is in that commit or earlier.

## READ THIS FIRST: the tree is not clean

**17 files are uncommitted and unverified.** They are the "a local beside a
`try`" work. The last `tools/verify-three` on them returned **rc=1** — two
Windows cases failed — and the cause was fixed but **the re-run never
happened**. `docs/EXCLUSIONS.md` was also reset and remapped afterwards.

So before building on this tree, do one of:

1. `tools/verify-three`, and commit if green; or
2. `git checkout -- .` and drop it, losing only the local-beside-try step.

Do not assume it works. The local suites were green *before* the last
exclusions correction, not after.

## The session in one line

Six commits, all three-box verified, and the theme was **oracles rather than
corpus size**: four green suites on three machines had been silent about
twenty-four separate gaps.

| commit | |
| --- | --- |
| `3f53ea1` | catching by reference — a slot holding the runtime's pointer |
| `14fd87e` | an alias template is C++11, and was refused as C++14 |
| `97fde25` | nine roots given named refusals |
| `6824b79` | a refusal is not a substitution failure |
| `2622513` | the last six of the widened sweep, and one was a real gap |
| `8681827` | class-typed exceptions and rethrow, three of the seven |

## The method that found all of it

**Classify by *how* the compiler refuses, not by whether the program
compiles.** One minimal program per feature, each first validated as conforming
by `clang++ -std=c++11 -pedantic-errors`, then sorted into three buckets:
compiles / refused **by name** / refused by a message naming no feature. The
third bucket is invisible: `tools/exclusions` derives its list from refusals
that say "not supported yet" or name a standard, so a generic `expected ';'`
can never be listed and never gets fixed.

105 probes over two grids found **24** such gaps. All are now named; both grids
read 63 compiling, 41 refused by name, **0 invisible**.

**The grid is not the standard.** The second grid found seven the first missed
purely by asking different questions — including two **C++98** features
(anonymous union, function try block). A third would find more. The user's
own C++ keyword list already surfaced one this way: see below.

## Open work, in the order I would take it

1. **Finish or drop the uncommitted work** (above).
2. **The alternative tokens** — `and or not xor compl bitand bitor and_eq
   or_eq xor_eq not_eq` are real C++ keywords, not C's macros. clang accepts
   them; cxx1 answers `expected ')'`, which is generic and therefore
   unlisted. Found from the user's keyword list, 2026-09-06, not started.
3. **A destructible object *inside* a `try` body or a handler.** Beside a
   `try` works now; inside does not — the cleanup region sits inside the row
   rather than beside it. See the ordering fact below before attempting it.
4. **`<stdexcept>`**, which needs 3 because its classes hold a `std::string`.
   That is the finish line: `catcher1.cpp` and `catcher2.cpp` in
   `~/Documents/Claude/lambdaTest` need exactly 3 and 4 and nothing else.
5. **Windows exception parity** — class throws and rethrow are Itanium-only.
   The Microsoft ThrowInfo chain is measured and written down in
   `tests/cases/throw-class.notarget`; the cleanup splitting has no Microsoft
   counterpart either.
6. `__cxa_free_exception` — cxx1 leaks the allocation if the copy into it
   throws. Recorded in `tests/cases/throw-class.nonames`.

## The one fact most likely to be re-broken

**The Itanium call-site rows are written innermost-first, not sorted by
address, and that is deliberate.** The personality routine scans linearly and
takes the *first* row whose range contains the address, so a region nested
inside another must be listed before it.

Sorting them by address looks obviously right, fixes a real problem (a region
inside a landing pad closes after the row that begins earlier), changes 14
emitted files, and **silently breaks unwinding through a nested scope**:

```cpp
A outer("outer");
{ A inner("inner"); boom(); }     // clang: ~inner ~outer     sorted: ~outer
```

All four suites stay green through that. What caught it was the **emit
golden**: it had read `0 of 675` all session, and the moment it did not, one
diff plus a purpose-written program found the leak, and `git stash` + rebuild
proved the regression was mine. The sort is reverted with a comment where it
was.

## Standing habits confirmed again this round

- **A refusal stricter than the standard is as wrong as one naming the wrong
  version.** `throw;` outside a handler is well-formed and terminating
  ([except.throw]/8); my first rethrow refused it.
- **A `.error` case must record the half both ABIs share** — Windows refuses a
  multi-base class for being a class at all, Itanium for the bases. The Windows
  box caught that, and the rule was already written in the old
  `throw-refused.cpp`.
- **Define a case's constructors out of line** or `names.sh` reports an
  emission difference (clang emits only C2 for inline) as though it were a
  mangling one. That happened twice more this round.
- **Remap `docs/EXCLUSIONS.md` once, from HEAD.** Remapping twice in a session
  against a moving baseline corrupts the citations; reset the file and remap
  from `git show HEAD:` in one pass.
